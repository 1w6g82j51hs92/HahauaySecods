-- Place this LocalScript in StarterGui
-- Updated: lighter frame, red outline, mild animation + hover effects

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local DISCORD_ICON_ASSET_ID = "rbxassetid://8248378219" -- replace with your decal id
local discordLink = "https://discord.gg/uAMwhmzAcA" -- put your actual invite here

-- ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "VeinyxGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui
screenGui.DisplayOrder = 100

-- Main frame sized like pasted.txt (40% width, 30% height)
local frame = Instance.new("Frame")
frame.Name = "MainFrame"
frame.AnchorPoint = Vector2.new(0.5, 0.5)
frame.Position = UDim2.new(0.5, 0, 0.35, 0)
frame.Size = UDim2.new(0.4, 0, 0.3, 0)
frame.BackgroundColor3 = Color3.fromRGB(28, 28, 30) -- lighter / soft black (pa-angassin)
frame.BorderSizePixel = 0
frame.Parent = screenGui

-- Red border (animated)
local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(255, 60, 60) -- red outline
stroke.Thickness = 3
stroke.Parent = frame

-- subtle inner glow (thin)
local innerStroke = Instance.new("UIStroke")
innerStroke.Color = Color3.fromRGB(255, 40, 40)
innerStroke.Thickness = 1
innerStroke.Transparency = 0.6
innerStroke.Parent = frame

-- Rounded corners
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = frame

-- Title/Label matching pasted.txt layout (fills top ~60% of frame)
local title = Instance.new("TextLabel")
title.Name = "Title"
title.Size = UDim2.new(1, 0, 0.6, 0)
title.Position = UDim2.new(0, 0, 0, 0)
title.BackgroundTransparency = 1
title.Text = "Luna Hub Script"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 22
title.TextWrapped = true
title.Parent = frame

-- Copy button (left) matching pasted.txt sizes and position
local copyBtn = Instance.new("TextButton")
copyBtn.Name = "CopyButton"
copyBtn.Size = UDim2.new(0.4, 0, 0.2, 0)
copyBtn.Position = UDim2.new(0.1, 0, 0.7, 0)
copyBtn.BackgroundColor3 = Color3.fromRGB(200, 40, 40) -- red-ish button
copyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
copyBtn.Font = Enum.Font.GothamSemibold
copyBtn.TextSize = 13
copyBtn.Text = "Copy Link Discord"
copyBtn.BorderSizePixel = 0
copyBtn.Parent = frame

local copyCorner = Instance.new("UICorner")
copyCorner.CornerRadius = UDim.new(0, 6)
copyCorner.Parent = copyBtn

-- Optional icon inside copyBtn (placeholder)
local icon = Instance.new("ImageLabel")
icon.Name = "DiscordIcon"
icon.Size = UDim2.new(0, 18, 0, 18)
icon.Position = UDim2.new(0, 8, 0.5, -9)
icon.BackgroundTransparency = 1
icon.Image = DISCORD_ICON_ASSET_ID
icon.Parent = copyBtn

-- Exit and Wait buttons (stacked in same spot; Wait visible first)
local exitBtn = Instance.new("TextButton")
exitBtn.Name = "ExitButton"
exitBtn.Size = UDim2.new(0.4, 0, 0.2, 0)
exitBtn.Position = UDim2.new(0.5, 0, 0.7, 0)
exitBtn.Text = "Exit"
exitBtn.Visible = false
exitBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
exitBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
exitBtn.Font = Enum.Font.GothamSemibold
exitBtn.TextSize = 13
exitBtn.BorderSizePixel = 0
exitBtn.Parent = frame

local exitCorner = Instance.new("UICorner")
exitCorner.CornerRadius = UDim.new(0, 6)
exitCorner.Parent = exitBtn

local waitBtn = Instance.new("TextButton")
waitBtn.Name = "WaittoExit"
waitBtn.Size = UDim2.new(0.4, 0, 0.2, 0)
waitBtn.Position = UDim2.new(0.5, 0, 0.7, 0)
waitBtn.Text = "Waiting Delay Exit"
waitBtn.Visible = true
waitBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
waitBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
waitBtn.Font = Enum.Font.GothamSemibold
waitBtn.TextSize = 13
waitBtn.BorderSizePixel = 0
waitBtn.Parent = frame

local waitCorner = Instance.new("UICorner")
waitCorner.CornerRadius = UDim.new(0, 6)
waitCorner.Parent = waitBtn

-- Status / fallback textbox (hidden by default)
local fallbackBox = Instance.new("TextBox")
fallbackBox.Name = "FallbackCopyBox"
fallbackBox.Size = UDim2.new(1, 0, 0.18, 0)
fallbackBox.Position = UDim2.new(0, 0, 0.78, 0)
fallbackBox.Visible = false
fallbackBox.ClearTextOnFocus = false
fallbackBox.Text = ""
fallbackBox.Font = Enum.Font.Gotham
fallbackBox.TextSize = 14
fallbackBox.TextColor3 = Color3.fromRGB(15, 23, 42)
fallbackBox.BackgroundColor3 = Color3.fromRGB(248, 250, 252)
fallbackBox.BorderSizePixel = 0
fallbackBox.Parent = frame

local fbCorner = Instance.new("UICorner")
fbCorner.CornerRadius = UDim.new(0, 6)
fbCorner.Parent = fallbackBox

-- Countdown behavior like your latest script: Wait 5 sec then swap buttons
task.delay(5, function()
    waitBtn.Visible = false
    exitBtn.Visible = true
end)

-- Copy functionality
copyBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        pcall(function()
            setclipboard(discordLink)
        end)
        copyBtn.Text = "Copied To Discord Link"
        task.wait(0.6)
        copyBtn.Text = "Copy Link Discord"
    else
        fallbackBox.Text = discordLink
        fallbackBox.Visible = true
        pcall(function() fallbackBox:CaptureFocus() end)
    end
end)

exitBtn.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.Escape then
        screenGui.Enabled = false
    end
end)

-- =======================
-- Animations (mild)
-- =======================
-- Pulse frame size slightly
local pulseTweenInfo = TweenInfo.new(
    1.2,
    Enum.EasingStyle.Sine,
    Enum.EasingDirection.InOut,
    -1, -- repeat forever
    true -- autoReverse
)
local biggerSize = UDim2.new(0.405, 0, 0.31, 0)
local pulseTween = TweenService:Create(frame, pulseTweenInfo, {Size = biggerSize})
pulseTween:Play()

-- Pulse stroke thickness for subtle glowing outline
local strokeTweenInfo = TweenInfo.new(1.2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true)
local strokeTween = TweenService:Create(stroke, strokeTweenInfo, {Thickness = 6})
strokeTween:Play()

-- Hover effects for buttons: brighten / scale
local function addHover(btn)
    local originalSize = btn.Size
    local hoverSize = UDim2.new(originalSize.X.Scale + 0.02, originalSize.X.Offset, originalSize.Y.Scale + 0.03, originalSize.Y.Offset)
    local hoverInfo = TweenInfo.new(0.16, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
    local leaveInfo = TweenInfo.new(0.16, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, hoverInfo, {Size = hoverSize}):Play()
        -- brighten background a little
        local bg = btn.BackgroundColor3
        local brighten = Color3.new(math.min(bg.r + 0.08,1), math.min(bg.g + 0.08,1), math.min(bg.b + 0.08,1))
        TweenService:Create(btn, hoverInfo, {BackgroundColor3 = brighten}):Play()
    end)

    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, leaveInfo, {Size = originalSize}):Play()
        TweenService:Create(btn, leaveInfo, {BackgroundColor3 = btn == copyBtn and Color3.fromRGB(200,40,40) or Color3.fromRGB(50,50,50)}):Play()
    end)
end

addHover(copyBtn)
addHover(waitBtn)
addHover(exitBtn)

-- small title shimmer (text transparency ping-pong)
local shimmerInfo = TweenInfo.new(1.6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true)
local shimmerTween = TweenService:Create(title, shimmerInfo, {TextTransparency = 0.12})
shimmerTween:Play()

-- done — mild animated, red outline, lighter (pa-angassin) look
